User authentication with jwt
Sign in User and signout User
Connect User to a websocket
Allow user to see a history of users contacted
Allow user to send message to another user
Allow user to see history of messages sent using lazy loading?

Redis for storing messages?
Jwt for authentication?


User can see a list of people he has chatted with in the past
User logs in and sees a list of people online(green icon) available people generally
USer selects one of them and sees all the messages exchanged

{
    'messages' : {
        user1 : "text", timestamp: "timestamp",
        user2 : "text", timestamp: "timestamp",
    }
}

Steps
chat_person_initiator_id
chat_person_recipient_id
Create a room called {initiator_id}__{recipient_id}_room
